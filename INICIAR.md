# Próxima sessão

## Primeiro objetivo

Criar clone compilável do `jogo-fps` com identidade própria, ainda sem editor.

1. Confirmar workspace `/host/home/apps/construa-jogue`.
2. Ler estes documentos e verificar mudanças no `jogo-fps`.
3. Copiar `src/`, sons, assets e scripts necessários; não copiar `.git/build`.
4. Renomear package para `br.com.termia.construajogue`.
5. Renomear app para `Construa & Jogue`.
6. Compilar e rodar teste de níveis.
7. Validar campanha atual no aparelho.
8. Criar `DIARIO.md` com o baseline.

Depois implementar apenas Fase 1: documento, catálogo, JSON, validação,
compilador e arena convertida. Não iniciar UI do editor antes de a arena JSON
continuar jogável.

